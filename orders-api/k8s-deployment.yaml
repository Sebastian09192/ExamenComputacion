apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-api
  namespace: cafe-aurora
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders-api
  template:
    metadata:
      labels:
        app: orders-api
    spec:
      containers:
      - name: orders-api
        image: orders-api:latest
        ports:
        - containerPort: 8000
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"
          requests:
            memory: "128Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: orders-api
  namespace: cafe-aurora
spec:
  selector:
    app: orders-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP